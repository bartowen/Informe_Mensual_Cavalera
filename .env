# Modo de datos: 'api' o 'static'
# 'api' = consume datos desde backend de Google Ads API
# 'static' = usa datos hardcodeados en mockData.ts (por defecto)
VITE_DATA_MODE=static

# API Backend URL (solo necesario si VITE_DATA_MODE=api)
# En desarrollo: http://localhost:3001
# En producción: https://tu-api.vercel.app o tu dominio
VITE_API_URL=http://localhost:3001

# Auto-refresh de datos (solo en modo API)
VITE_AUTO_REFRESH=false
VITE_AUTO_REFRESH_INTERVAL=30

# URL del Google Apps Script (para el API route /api/cavalera-metrics)
# Obtén esta URL publicando tu Google Apps Script como Web App
GOOGLE_APPS_SCRIPT_URL=
